import { TCardinalRotations } from "./types";

export const DEFAULT_TIME = 180;
export const BOARD_SIZE = 5;
export const MINIMUM_WORD_LENGTH = 4;
export const L_RANGE = {
  MIN: 30,
  MAX: 90,
}
export const ROTATION_DEG = [
  TCardinalRotations.UP,
  TCardinalRotations.DOWN,
  TCardinalRotations.LEFT,
  TCardinalRotations.RIGHT
]
export const UNDERLINED_STRING = ['W', 'M']